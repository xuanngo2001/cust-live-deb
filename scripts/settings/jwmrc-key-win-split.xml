  
  <Key mask="4" key="Left">exec:/usr/local/bin/cld-win-split.sh</Key>         <!-- `⌘ + Left` : Move active window to the left half section of the screen. -->
  <Key mask="4" key="Right">exec:/usr/local/bin/cld-win-split.sh right</Key>  <!-- `⌘ + Right`: Move active window to the right half section of the screen. -->
  <Key mask="4" key="Up">exec:/usr/local/bin/cld-win-split.sh right</Key>     <!-- `⌘ + Up`: Move active window to the top half section of the screen. -->
  <Key mask="4" key="Down">exec:/usr/local/bin/cld-win-split.sh</Key>         <!-- `⌘ + Down` : Move active window to the bottom half section of the screen. -->
  
  
  <Key mask="4" key="Right">exec:/usr/local/bin/cld-win-split.sh right</Key>  <!-- `⌘ + Right`: Move active window to the right half side of the screen. -->
  <Key mask="4" key="Left">exec:/usr/local/bin/cld-win-split.sh</Key>         <!-- `⌘ + Left` : Move active window to the left half side of the screen. -->

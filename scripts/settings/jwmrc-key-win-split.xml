  <Key mask="CA" key="Left">exec:/usr/local/bin/cld-win-split.sh left</Key>    <!-- `Ctrl + Alt + Left` : Maximize active window to the left half section of the screen. -->
  <Key mask="CA" key="Right">exec:/usr/local/bin/cld-win-split.sh right</Key>  <!-- `Ctrl + Alt + Right`: Maximize active window to the right half section of the screen. -->
  <Key mask="CA" key="Up">exec:/usr/local/bin/cld-win-split.sh top</Key>       <!-- `Ctrl + Alt + Up`: Maximize active window to the top half section of the screen. -->
  <Key mask="CA" key="Down">exec:/usr/local/bin/cld-win-split.sh bottom</Key>  <!-- `Ctrl + Alt + Down` : Maximize active window to the bottom half section of the screen. -->

  <!-- KP_7 is not the 7 above the letter. KP_7 is the 7 on the numpad. -->
  <Key mask="CA" key="KP_7">exec:/usr/local/bin/cld-win-split.sh 7</Key>    <!-- `Ctrl + Alt + 7` : Move active window to the top-left section of the screen. -->
  <Key mask="CA" key="KP_8">exec:/usr/local/bin/cld-win-split.sh 8</Key>    <!-- `Ctrl + Alt + 8` : Move active window to the top-center section of the screen. -->
  <Key mask="CA" key="KP_9">exec:/usr/local/bin/cld-win-split.sh 9</Key>    <!-- `Ctrl + Alt + 9` : Move active window to the top-right section of the screen. -->

  <Key mask="CA" key="KP_4">exec:/usr/local/bin/cld-win-split.sh 4</Key>    <!-- `Ctrl + Alt + 4` : Move active window to the middle-left section of the screen. -->
  <Key mask="CA" key="KP_5">exec:/usr/local/bin/cld-win-split.sh 5</Key>    <!-- `Ctrl + Alt + 5` : Move active window to the center of the screen. -->
  <Key mask="CA" key="KP_6">exec:/usr/local/bin/cld-win-split.sh 6</Key>    <!-- `Ctrl + Alt + 6` : Move active window to the middle-right section of the screen. -->

  <Key mask="CA" key="KP_1">exec:/usr/local/bin/cld-win-split.sh 1</Key>    <!-- `Ctrl + Alt + 1` : Move active window to the bottom-left section of the screen. -->
  <Key mask="CA" key="KP_2">exec:/usr/local/bin/cld-win-split.sh 2</Key>    <!-- `Ctrl + Alt + 2` : Move active window to the bottom-center of the screen. -->
  <Key mask="CA" key="KP_3">exec:/usr/local/bin/cld-win-split.sh 3</Key>    <!-- `Ctrl + Alt + 3` : Move active window to the bottom-right section of the screen. -->

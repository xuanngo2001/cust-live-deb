
# Define what to display on the command line: e.g. --=[2015-05-26 14:00:32 @root]=--
# http://askubuntu.com/a/24422
red=$(tput setaf 1)
green=$(tput setaf 2)
reset=$(tput sgr0)
bold=$(tput bold)
export PS1="\[$bold\]\[$green\]--=[$(date +%Y-%m-%d) \t @\[$red\]\u\[$green\]]=--\n\[$red\](live)\[$reset\]\w>"

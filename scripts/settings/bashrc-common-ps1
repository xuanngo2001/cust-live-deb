
# Define what to display on the command line: e.g. --=[2015-05-26 14:00:32 @root]=--
# http://askubuntu.com/a/24422
red=$(tput setaf 1)
white=$(tput setaf 7)
reset=$(tput sgr0)
bold=$(tput bold)
export PS1="\[$bold\]\[$white\]--=[$(date +%Y-%m-%d) \t @\[$red\]\u\[$white\]]=--\n\[$red\](live)\[$reset\]\[$bold\]\w>\[$reset\]"

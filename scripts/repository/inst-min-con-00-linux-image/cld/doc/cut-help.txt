# If you use space as delimiter, you want to treat multiple spaces as one.
#   Otherwise, you will get multiple -f.
    tr -s ' ' | cut -d ' ' -f 2
    awk '{print $2}'

# Cut using TAB delimiter.
    cut -d$'\t' -f2

# Remove 3rd column.
    cut -d';' --complement -f3

# Use any whitespace character as delimiter.
    tr '[:space:]' ' ' | tr -s ' ' | cut -d' ' -f1

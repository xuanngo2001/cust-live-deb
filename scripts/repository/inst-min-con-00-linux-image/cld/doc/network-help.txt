# Show which interfaces are wireless and which ones are not.
#   no wireless extensions = Not wireless interface.
	iwconfig

# List processes using the port number.
    lsof -i:80

# List processes using port 80
    netstat -pna | grep 80

# What ports are in used.
    netstat -tulpn | grep LISTEN
    ss -tulpn | grep LISTEN
    lsof -i -P -n | grep LISTEN
    lsof -i:22 ## see a specific port such as 22 ##
    
# List all of your interfaces
    ifconfig -a
    ip link show
    cat /proc/net/dev

    # Naming 
        Two character prefixes based on the type of interface:
         *   en -- ethernet
         *   sl -- serial line IP (slip)
         *   wl -- wlan
         *   ww -- wwan
 
# List network cards
    lspci | grep -E -i --color 'network|ethernet|wireless|wi-fi'

# Convert a string into an array.
    arr=($line)
    read -a arr <<< $line

    for i in ${arr[@]}; do echo $i; done

# Heredoc
read -r -d '' FRON_MATTER <<- EOM
+++
title = '${title}'
date = ${date_iso}
draft = false
+++

EOM

# Read stdin, read data passing to pipe
    while read line
    do 
        echo "${line}" | \
            sed 's/  ##    .*//' | \
            sed 's/##xn##.*//' | \
            xargs
    done

# Ask question. Prevent accidental delete without backup.
    cr=`echo $'\n.'`
    cr=${cr%.}
    while true; do
        read -p "Are you sure to overwrite cron/ on other projects?${cr}$(cat "${this_script}.txt.sh" | grep -v '^#')${cr}" yn
        case $yn in
            [Yy]* ) break;;
            [Nn]* ) exit;;
            * ) echo "Please answer yes or no.";;
        esac
    done
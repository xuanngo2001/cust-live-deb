# Build
    # Run commands from a script file. Don't know why it doesn't work outside of file.
    # Build the *.deb files.
    # Install them.
    
    # Don't call the build script from CLD. Otherwise, jwm is removed. Don't why.

